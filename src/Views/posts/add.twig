{# Add action template for Posts controller #}
{% extends 'base.twig' %}

{% block title %}{{ 'Add post'|trans }}{% endblock %}

{% block heading %}{{ 'Add post'|trans }}{% endblock %}

{% block content %}

    <div class="container">
        <div class="row">
            <div class="col-lg-8 col-lg-offset-2 col-md-10 col-md-offset-1">
                <form action="{{ url('post_add') }}" method="post">
                    {{ form_widget(form) }}
                    <input type="submit" name="submit" class="btn btn-success" value="{{ 'Save'|trans }}" />
                </form>
            </div>
        </div>
    </div>


{% endblock %}